---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
{{ include "tekton_dashboard.labels" . | indent 4 }}
  name: {{ template "tekton_dashboard.fullname". }}-dashboard
  namespace: {{ .Release.Namespace }}
