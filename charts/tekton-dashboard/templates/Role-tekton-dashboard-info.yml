---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
{{ include "tekton_dashboard.labels" . | indent 4 }}
  name: {{ template "tekton_dashboard.fullname". }}-info
  namespace: {{ .Release.Namespace }}
rules:
  - apiGroups:
      - ""
    resourceNames:
      - {{ template "tekton_dashboard.fullname". }}-info
    resources:
      - configmaps
    verbs:
      - get
